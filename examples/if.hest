isCondBoolean = ?(cond || True)
result = if isCondBoolean
    then if cond
        then 333
        else 666
    else undefined

result1 = if (isCondBoolean && cond) ?: False
    then 333
    else 666
